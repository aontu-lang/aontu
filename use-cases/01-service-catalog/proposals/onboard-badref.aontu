# proposals/onboard-badref.aontu --- the same onboarding flow, but the
# agent's candidate depends on a path no file writes. The rel() check
# fails the whole evaluation.
@"../system.aontu"

candidate: hide(@"../data/candidate-badref.aontu")

catalog: domains: platform: services: search: $.spec.CatalogEntry & $.candidate
