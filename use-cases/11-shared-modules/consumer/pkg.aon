# The checkout team's project: what it wants, not what it has.
# `aontu sync` turns this into aontu_meta/pkg-lock.aon and materialises
# the closure into aontu_meta/vendor/ (committed).
pkg: { path:"corp.example/checkout" main:"main.aon" }
dep: "corp.example/schemas/service": v: "1.4.2"
