# BUGS.md section 35a -- FIXED 2026-08-26 by the template-clone
# isolation change (ADR-005): the same instance path normalisation
# that fixed section 33 makes a sibling reference inside a
# spread-template EXPRESSION answer per child. Historically
# `&: {md: .side_effect}` worked but `&: {md: "|" + .side_effect}`
# failed with no_path.
# Pinned in both engines: test/spec/spread.tsv spread-expr-sibling-ref.
# expected: {"rows":{"a":{"md":"|readonly","side_effect":"readonly"}}} exit 0
# actual:   {"rows":{"a":{"md":"|readonly","side_effect":"readonly"}}} exit 0
rows: { &: { md: "|" + .side_effect } a: { side_effect: readonly } }
