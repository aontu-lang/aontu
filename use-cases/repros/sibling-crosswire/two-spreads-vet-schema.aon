# FIXED 2026-08-26 (the spread application rework: pure ExpectVal).
# vet form (use-case 02 gap 6): two spreads addressing one map at
# DIFFERENT depths; vetting correct data used to report one sibling's
# data value conflicting with another sibling's data value.
# expected: verdict: valid, exit 0
# actual: verdict: valid, exit 0
# (run: aontu vet two-spreads-vet-schema.aon two-spreads-vet-data.json)
# Pinned in the shared spec: vet.tsv vet-unequal-spread-depths.
a: &: { w: &: { p: integer } }
a: prod: w: &: { r: integer }
