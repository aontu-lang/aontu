# The model WITHOUT the overlay: this is the --entry for `aontu set`.
# It must never include overlay.aon -- an entry that includes the
# overlay makes every set conflict with itself (see docs/how-to.md).
@"./flags.aon"
@"./layers.aon"
@"./policy.aon"
