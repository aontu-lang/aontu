# The formerly nastier half of gap 6 — 2026-08-26: FIXED by the
# template-clone isolation change (ADR-005). With id(key(0)) removed,
# the include+alias combination now emits the record correctly.
# Historically it SILENTLY DROPPED the record (exit 0, customers {}):
# the reference cloned the still-pending type() alias and the clone
# stamped its type mark at the destination after the reference's
# mark-clearing walk had run. README, gap 6; shared spec pin:
# test/spec/file.tsv load-alias-spread.
@"./vocab-noid.aon"
customers: "cust-1001": { ledgerId: 5, name: "Acme" }
