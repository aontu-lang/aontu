# Minimal repro, file 2 of 2 — 2026-08-26: FIXED by the template-clone
# isolation change (ADR-005). Pulling the same schema in through an
# include and adding one record now emits the fully-unified record
# with its id(key(0)) identity, exactly as the direct evaluation
# always did. Historically this failed with a bogus [aontu/id_name]
# "argument is not an entity name" (a leaked type mark froze the
# pending key(0) and id() saw the unevaluated call). README, gap 6;
# shared spec pin: test/spec/file.tsv load-alias-idspread.
@"./vocab.aon"
customers: "cust-1001": { ledgerId: 5, name: "Acme" }
