# The enforcing spelling: a plain disjunction refuses "superadmin"
# ([aontu/|:empty]) -- but the default is gone, so this file no longer
# evaluates on its own (generation fails on the open disjunction).
# Enforcement and a default are mutually exclusive today.
invite: { role: member | admin | owner }
