{
  "name": "aontu-playground-build",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling for the aontu web playground: bundles ts/dist for the browser.",
  "scripts": {
    "build": "node build.mjs",
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
